<beans:beans xmlns="http://www.springframework.org/schema/security"
    xmlns:beans="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans 
                    http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
                    http://www.springframework.org/schema/security
                    http://www.springframework.org/schema/security/spring-security-3.0.3.xsd">
                    
                    
                    
<beans:bean id="entryPoint" class="org.slc.sli.security.SLIAuthenticationEntryPoint">
    <beans:property name="restClient" ref="restClient" />
    <beans:property name="clientSecret" value="${oauth.client.secret}" />
    <beans:property name="clientId" value="${oauth.client.id}" />
    <beans:property name="callbackUrl" value="${oauth.redirect}" />
    <beans:property name="apiUrl" value="${api.server.url}" />
    
</beans:bean>                    

<http create-session="never" auto-config="false" use-expressions="true" entry-point-ref="entryPoint">

    <intercept-url pattern="/static/**" filters="none"/>    
    <intercept-url pattern="/" access="hasRole('Educator')"/>
    <intercept-url pattern="/appselector" access="hasRole('Educator')"/>
    <intercept-url pattern="/studentlist" access="hasRole('Educator')"/>
    <intercept-url pattern="/studentlistcontent" access="hasRole('Educator')"/>
    <intercept-url pattern="/service/**" access="hasRole('Educator')"/>
    <intercept-url pattern="/studentprofile" access="hasRole('Educator')"/>
    <intercept-url pattern="/**" access="denyAll" />
    <anonymous username="anonymous" granted-authority="ROLE_ANONYMOUS" />
    <logout/>
</http>

<authentication-manager alias="authenticationManager" />

</beans:beans>
