<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.slc.sli.ingestion</groupId>
    <artifactId>ingestion</artifactId>
    <version>1.0-SNAPSHOT</version>
  </parent>

  <artifactId>ingestion-service</artifactId>
  <packaging>war</packaging>

  <name>Ingestion Service</name>

  <dependencies>

    <dependency>
      <groupId>org.slc.sli.ingestion</groupId>
      <artifactId>ingestion-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-instrument-tomcat</artifactId>
      <version>${spring.version}</version>
      <scope>compile</scope>
    </dependency>

  </dependencies>

  <properties>
    <sli.home>${project.parent.basedir}/..</sli.home>
  </properties>


  <build>
    <finalName>ingestion-service</finalName>

    <plugins>

      <plugin>
        <groupId>org.codehaus.cargo</groupId>
        <artifactId>cargo-maven2-plugin</artifactId>
        <version>1.2.2</version>
        <configuration>
          <container>
            <containerId>tomcat7x</containerId>

            <zipUrlInstaller>
              <url>http://archive.apache.org/dist/tomcat/tomcat-7/v7.0.27/bin/apache-tomcat-7.0.27.zip</url>
            </zipUrlInstaller>

            <dependencies>
              <dependency>
                <groupId>org.springframework</groupId>
                <artifactId>spring-instrument-tomcat</artifactId>
              </dependency>
            </dependencies>

            <systemProperties>
              <sli.conf>${sli.conf}</sli.conf>
              <sli.env>local</sli.env>
              <sli.encryption.keyStore>${project.parent.basedir}/../data-access/dal/keyStore/localKeyStore.jks</sli.encryption.keyStore>
            </systemProperties>
          </container>

          <configuration>
            <properties>
              <cargo.jvmargs>-Xms1g -Xmx1g -Xss4m -XX:PermSize=384m -XX:MaxPermSize=384m -XX:MaxHeapFreeRatio=70 -XX:+UseParallelGC -XX:ParallelGCThreads=20</cargo.jvmargs>
            </properties>
          </configuration>
        </configuration>
      </plugin>

    </plugins>

  </build>


</project>
