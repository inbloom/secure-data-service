require 'json'
require_relative '../../utils/sli_utils.rb'

Given /^my District is "([^"]*)"$/ do |arg1|
  # No code needed, this step is informational
end

When /^I try to access the data for "([^"]*)" in my district from the API$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Then /^I get the data returned in json format$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I try to access the data for "([^"]*)" in another district from the API$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Then /^the data should be updated$/ do
  pending # express the regexp above with the code you wish you had
end

Then /^the data should not have changed$/ do
  pending # express the regexp above with the code you wish you had
end

When /^I try to access the data for "([^"]*)" in my school from the API$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^I try to access the data for "([^"]*)" in another school from the API$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

Given /^my State is "([^"]*)"$/ do |arg1|
  # No code needed, this step is informational
end

When /^I try to access the data for "([^"]*)" in my state from the API$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end

When /^I try to access the data for "([^"]*)" in another state from the API$/ do |arg1|
  pending # express the regexp above with the code you wish you had
end
