# Read about fixtures at http://api.rubyonrails.org/classes/ActiveRecord/Fixtures.html

# This model initially had no columns defined.  If you add columns to the
# model remove the '{}' from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
one:
    id: "1"
    name: Shared Learning Initiative
    edOrg: 
    regionId: http://slidev.org
    uniqueIdentifier: realm-one
    idp:
      id: http://slidev.org
      redirectEndpoint: http://slidev.org/endpoint
    realm: "dc=slidev,dc=org"
    mappings: {"Educator": ["teacher", "ta"]}

# column: value
#
two:
    id: "2"
    name: Tardis
    edOrg: "Waffles"
    regionId: http://timelords.com 
    uniqueIdentifier: realm-two
    idp: 
      id: http://timelords.com
      redirectEndpoint: http://timelords.com/gallifrey
    realm: "dc=timelors,dc=org"
    mappings: {"Educator": ["Educator"], "Leader": ["Leader"]}
#  column: value
