<%= form_for(@admin_delegation) do |f| %>
  <% if @admin_delegation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_delegation.errors.count, "error") %> prohibited this admin_delegation from being saved:</h2>

      <ul>
      <% @admin_delegation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
