<div id="notice">
	<%= notice %>
</div>
<div class="page-header">
<h1>Custom Roles for <%= @realm.name %> </h1>
</div>

<%= form_for(@realm, :url => {:controller => 'custom_roles', :action => 'update'}) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

