<%=
html = ""
if entity.empty?
	html << "Entity is empty"
else
    if entity["entityType"] == "educationOrganization"
        html << display_edorg_table(entity)
    end
	html << display_entity(entity)
end
html.html_safe
%>
